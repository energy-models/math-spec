# SPDX-FileCopyrightText: math-spec Contributors
#
# SPDX-License-Identifier: MIT

[workspace]
name = "math-spec"
channels = ["conda-forge"]
# No `exclude-newer`. A rolling supply-chain delay and `preview =
# ["pixi-build"]` cannot both hold: the build backend this workspace needs is
# resolved from conda-forge like any other package, and the only
# `pixi-build-python` matching a current pixi's backend API is days old. A 7-day
# cutoff excludes it, and CI goes red with no commit behind it — which is what
# happened here between 2026-08-17 (green) and 2026-08-21 (red) on this very
# commit. Windows of 3d, 5d, 6d and 7d were all tried; only "now" resolves.
platforms = ["osx-arm64", "osx-64", "linux-64", "win-64"]
preview = ["pixi-build"]

[package]
name = "math-spec"
[package.build.backend]
name = "pixi-build-python"
version = "*"
[package.host-dependencies]
python = ">=3.12"
hatchling = "*"
uv = "*"

[dependencies]
math-spec = { path = "." }

[feature.test.dependencies]
pytest = "*"
pytest-cov = "*"
mypy = "*"
[feature.test.tasks]
test = "pytest"
test-coverage = "pytest --cov=math_spec --cov-report=xml --cov-report=term-missing"

[feature.build.dependencies]
python = "*"
hatchling = "*"
python-build = "*"
twine = ">=6"
[feature.build.tasks]
build-wheel = "python -m build --no-isolation ."
check-wheel = "twine check dist/*"

[feature.lint.dependencies]
lefthook = "*"
ruff = "*"
prettier = "*"
taplo = "*"
pre-commit-hooks = "*"
typos = "*"
zizmor = "*"
reuse = "*"
[feature.lint.tasks]
pre-commit-install = "lefthook install"
lint = "lefthook run pre-commit --all-files"

[feature.docs.dependencies]
mkdocs = ">=1.6,<2"
mkdocs-material = ">=9.5"
mkdocstrings = ">=0.29"
mkdocstrings-python = ">=1.16"
mkdocs-bibtex = ">=2.16"
pymdown-extensions = ">=10.0"
mkdocs-macros-plugin = ">=1.0"
mkdocs-table-reader-plugin = ">=3.1"
[feature.docs.tasks.docs-serve]
cmd = "mkdocs serve"
[feature.docs.tasks.docs-build]
# `{{ dir }}` is pixi's own arg template and must reach pixi intact. The
# upstream copier template writes it in a .jinja file without a `raw` block, so
# copier interpolates it at generation and every repo built from the template
# gets `-d /html` — an absolute path at the filesystem root, with the declared
# `dir` argument silently discarded. That is why Read the Docs finds no output.
cmd = "mkdocs build -d {{ dir }}/html"
args = ["dir"]

[feature.template.dependencies]
copier = ">=9.16"
[feature.template.tasks.template-update]
cmd = "copier update --trust --skip-answered --skip-tasks ."

[feature.py312.dependencies]
python = "3.12.*"
[feature.py313.dependencies]
python = "3.13.*"
[feature.py314.dependencies]
python = "3.14.*"

[environments]
default = ["test", "build", "lint", "docs"]
template = ["template"]
py312 = ["py312", "test"]
py313 = ["py313", "test"]
py314 = ["py314", "test"]
