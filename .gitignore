__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.eggs/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
*.so
.env
.venv/
venv/
.idea/
uv.lock

# the built site; `site_dir` in mkdocs.yml is `.docs`, and `site/` is mkdocs'
# own default for anyone who builds with -d left off. Read the Docs builds it
# from source, so neither is committed.
.docs/
site/
# what the docs plugins cache between builds
.cache/

# macOS
.DS_Store

# direnv, for anyone who uses it to activate .venv on cd
.direnv/
.envrc

# JetBrains writes these; a shared code style is not one of them.
.idea/**/codeStyles/
