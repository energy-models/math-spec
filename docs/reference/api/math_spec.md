<!--
SPDX-FileCopyrightText: math-spec contributors
SPDX-License-Identifier: CC-BY-4.0
-->

# math_spec

Every name the package promises, rendered from its own docstrings. A name
reaches this page by joining `math_spec.__all__`, and
[what counts as public API](../../about/what-counts-as-public-api.md) says what
a name has to hold to be allowed in.

::: math_spec

<!-- `advice` is both a name in `__all__` and a module of the package.
     mkdocstrings resolves the collision to the module and renders neither, so
     the function is asked for by its full path. -->

::: math_spec.advice.advice
    options:
      show_root_heading: true
      show_root_full_path: false
