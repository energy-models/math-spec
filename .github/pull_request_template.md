<!-- The PR title is the changelog entry. It must be a conventional commit
     subject — `feat:`, `fix:`, `docs:`, … — or the change is silently missing
     from CHANGELOG.md. The `PR title` check enforces this; see RELEASING.md.

     While the version is pinned to the alpha stream, a breaking marker (`!`,
     or a `BREAKING CHANGE:` footer) is refused: it moves the base version
     rather than the alpha counter. Describe the break here instead. -->

## What this changes

## Why
